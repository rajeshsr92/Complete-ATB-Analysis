# Pickle caches under Data/ — generated at runtime, never commit
Data/**/*.pkl

# Client production & work queue files — live data, never commit xlsx
Data/*/Production/*.xlsx

# Legacy flat Production folder (kept for compat, not tracked)
Data/Production/

# Build artifacts
ATB_Dashboard.spec
ATB_Dashboard_Showcase.zip
build/
dist/

# Compiled Python libraries (installed locally, not part of source)
lib/

# Pickle caches generated at runtime
*.pkl
*.atb_*.pkl
*.medicare*.pkl

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Environment / secrets
.env
*.env

# OS junk
.DS_Store
Thumbs.db
desktop.ini
